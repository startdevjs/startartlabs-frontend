import { Body } from "./styles";
import Loading from "react-loading";

const LoadingComponent = () => {
  return (
    <Body>
      <Loading type="balls" color="#a9a9a9" />
    </Body>
  );
};

export default LoadingComponent;
